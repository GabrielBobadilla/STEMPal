Fix Vercel backend - install root deps and fix serverless function

- Root package.json updated with Supabase and backend deps
- Removed stale MySQL deps (mysql2, bcryptjs, jsonwebtoken, sql.js)
- vercel.json: install frontend deps too, increase function memory
