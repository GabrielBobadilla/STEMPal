Fix loading spinner stuck on Vercel and hardcoded localhost URLs

- AuthContext init() now wrapped in try/catch so loading always resolves
- Profile.jsx profile picture URL uses relative path instead of localhost:5000
